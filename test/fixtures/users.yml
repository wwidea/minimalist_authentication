<% shared_salt = User.make_token %>

active_user:
  email:                  test@testing.com
  active:                 true
  crypted_password:       <%= User.secure_digest('password', shared_salt) %>
  salt:                   <%= shared_salt %>

legacy_user:
  email:                  legacy@user.com
  active:                 true
  crypted_password:       <%= User.secure_digest('my_password', shared_salt) %>
  salt:                   <%= shared_salt %>
